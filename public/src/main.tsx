// Intentionally empty module.
//
// This project is a ZERO-BUILD Three.js game: the real entry point is
// /game.js (loaded via the importmap in index.html). The React/Vite entry
// tag is kept in index.html for tooling compatibility, and this stub is
// copied into dist/src/main.tsx by the build sync so the tag never 404s
// and never throws in the browser.
export {}
